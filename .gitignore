# 依赖包（很大，不上传，别人用 npm install 自己装）
node_modules

# 打包产物
dist
dist-ssr

# 各种日志和缓存
*.log
.DS_Store
.vite

# 本地环境变量（以后放 API key 用，绝对不能上传）
.env
.env.local

# 记忆后端
server/Ombre-Brain
server/.env
server/node_modules
